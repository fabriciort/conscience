import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Consciência - Plataforma Educacional de Assinatura",
  description: "Plataforma educacional com conteúdo exclusivo, mentoria e recursos de aprendizagem para Pesquisadores, Profissionais, Estudantes e Empresas.",
  keywords: ["educação", "mentoria", "assinatura", "aprendizado", "pesquisa"],
  authors: [{ name: "Consciência" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="pt-BR" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
